node_modules

# Output
.output
.vercel
.netlify
.wrangler
/.svelte-kit
/build
/dist

# OS
.DS_Store
Thumbs.db

# Env
.env
.env.*
!.env.example
!.env.test

# Vite
vite.config.js.timestamp-*
vite.config.ts.timestamp-*

# Zones pending catalog (task 77): local dev runtime directory
# (data/zones-pending relative to cwd) — closed upload queue, not a source asset
/data
